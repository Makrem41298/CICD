name: 02-basic-workflow

on:
  workflow_dispatch:

env:
  XX: "test"

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    if: ${{ env.XX=='test' }}
    steps:
      - name: Test
        run: echo "test valid"
