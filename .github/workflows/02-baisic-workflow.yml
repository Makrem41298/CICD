name: 02-baisic-workflow.yml
on:
  workflow_dispatch:

jobs:
  print-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World 2
        run:  exit 1
      - name: test
        if: ${{success()&&false||true}}
        run: echo " test valid"
